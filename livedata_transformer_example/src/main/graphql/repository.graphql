query Repository($ownerId:String!,$repoName:String!) {
 repository(owner: $ownerId, name: $repoName) {
    isFork
    name
    description
    url
    labels(first: 25) {
      nodes {
        name
      }
    }
  }
}
